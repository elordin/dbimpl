#include <string>
#include <cstdint>
#include <cassert>
#include <vector>

#include "Operators/Register.hpp"

int main(int argc, char* argv[]) {
    // Get command line argument
	// Test for Integer and String
	// Test-Example: Print(HashJoin(Projection(Select(R, A, 5), B), S))

   return 0;
}
